version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: massakone/reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: massakone/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: massakone/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: massakone/udagram-frontend:local