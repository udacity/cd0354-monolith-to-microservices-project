apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend-service
  namespace: default
  labels:
    app: udagram-frontend
spec:
  ports:
  - protocol: TCP
    port: 8100
    targetPort: 80
  selector:
    service: udagram-frontend