version: "3.3" # https://kompose.io/conversion/
services:
  reverseproxy:
    image: reverseproxy
    ports:
      - 8080:8080
    restart: always
    depends_on:
      - backend-user
      - backend-feed
  backend-user:
    image: udagram-api-user
    # volumes: # delete me
    #   - ./udagram-api-user:/usr/src/app # delete me
    environment:
      POSTGRES_USERNAME: $POSTGRES_USERNAME
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_DB: $POSTGRES_DB
      POSTGRES_HOST: $POSTGRES_HOST
      JWT_SECRET: $JWT_SECRET
      URL: "http://localhost:8100"
    # depends_on: # delete me
    #   - postgres # delete me
  backend-feed:
    image: udagram-api-feed
    # volumes: # delete me
    #   - ./udagram-api-feed:/usr/src/app # delete me
    environment:
      POSTGRES_USERNAME: $POSTGRES_USERNAME
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_DB: $POSTGRES_DB
      POSTGRES_HOST: $POSTGRES_HOST
      AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID
      AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY
      AWS_REGION: $AWS_REGION
      AWS_BUCKET: $AWS_BUCKET
      JWT_SECRET: $JWT_SECRET
      URL: "http://localhost:8100"
    # depends_on: # delete me
    #   - postgres # delete me
  frontend:
    image: udagram-frontend:local
    ports:
      - "8100:80"
    labels:
      kompose.service.expose: true
  # DELETE BELOW
  # eksctl create cluster --name udagram-tahina --region us-east-2 --version 1.23 --nodegroup-name udagram-nodes --node-type t2.micro --nodes 2
  # eksctl create cluster --name udagram-tahina --region us-east-2 --version 1.23 --nodegroup-name udagram-nodes --node-type t2.micro --nodes 2
# kubectl apply -f backend-feed-deployment.yml -f backend-user-deployment.yml -f frontend-deployment.yml -f reverseproxy-deployment.yml
# kubectl apply -f backend-feed-service.yml -f backend-user-service.yml -f frontend-service.yml -f reverseproxy-service.yml

  # postgres:
  #   image: postgres:14-alpine
  #   volumes:
  #     - ./data/db:/var/lib/postgresql/data
  #   ports:
  #     - 5432:5432
  #   environment:
  #     POSTGRES_USER: $POSTGRES_USERNAME
  #     POSTGRES_PASSWORD: $POSTGRES_PASSWORD
  #     POSTGRES_DB: $POSTGRES_DB
